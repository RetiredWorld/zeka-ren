declare namespace MyTheme {
  let themeMode: import('../components/public/tools').IThemeMode;
  const themeBackground: {
    dark: string,
    light: string,
  };
  const backgroundEle: HTMLDivElement;
}
